package main;

import implementations.ContainerDaoImpl;
import implementations.CustomerDaoImpl;
import implementations.DAOFactory;
import model.Container;
import model.Customer;
import model.User;

public class mainCLass {
	/*public static void main(String[] args){
		DAOFactory df=DAOFactory.getInstance();
		Customer customer=new Customer("llogin","MD5","gold");
		new CustomerDaoImpl(df).persistCustomer(customer);
		Container c=new Container("nname",1,2,"sstatus","iip",customer);
		new ContainerDaoImpl(df).persistContainer(c);
	}*/
}
